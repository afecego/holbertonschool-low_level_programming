#include "holberton.h"
#include <stdio.h>

/**
 * _abs - check the code for Holberton School students.
 * @i: Parameter of the funtion
 * Return: Always 0.
 */

int _abs(int i)
{
if (i >= 0)
{
return (i);
}
else
{
return (-i);
}
}
